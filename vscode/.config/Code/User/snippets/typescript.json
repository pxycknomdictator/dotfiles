{
  "handle function": {
    "prefix": "handle",
    "body": [
      "export async function handle({ resolve, event }) {",
      "    return await resolve(event);",
      "}",
      ""
    ],
    "description": "handle function"
  }
}
